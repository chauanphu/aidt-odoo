<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Deliberately NOT noupdate: record rules are a security control and the
         code must stay the source of truth. Xem aidt_org/security/aidt_org_rules.xml. -->
    <data>
        <record id="calendar_event_rule_secrecy" model="ir.rule">
            <field name="name">Calendar Event Secrecy Access Rule</field>
            <field name="model_id" ref="calendar.model_calendar_event"/>
            <field name="domain_force">[('secrecy_level', '&lt;=', user.clearance_level)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
