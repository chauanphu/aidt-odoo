<?xml version="1.0" encoding="utf-8"?>
<odoo>
<!-- Deliberately NOT noupdate: record rules are a security control and the code
     must stay the source of truth. With noupdate="1" a changed domain_force
     never reaches an already-installed database — an upgrade silently keeps the
     old rule, so a tightened rule would never take effect in production. -->
<data>

    <record id="rule_aidt_document_scope" model="ir.rule">
        <field name="name">Văn bản: đọc trong đơn vị mình + được chia sẻ</field>
        <field name="model_id" ref="model_aidt_document"/>
        <field name="groups" eval="[(4, ref('group_chuyen_vien'))]"/>
        <field name="domain_force">['|', ('department_id', '=', False), '|', ('department_id', 'child_of', user.employee_id.department_id.ids), ('shared_user_ids', 'in', [user.id])]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="rule_aidt_document_scope_write" model="ir.rule">
        <field name="name">Văn bản: ghi chỉ trong đơn vị mình</field>
        <field name="model_id" ref="model_aidt_document"/>
        <field name="groups" eval="[(4, ref('group_chuyen_vien'))]"/>
        <field name="domain_force">['|', ('department_id', '=', False), ('department_id', 'child_of', user.employee_id.department_id.ids)]</field>
        <field name="perm_read" eval="False"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <record id="rule_aidt_document_admin" model="ir.rule">
        <field name="name">Văn bản: quản trị toàn quyền</field>
        <field name="model_id" ref="model_aidt_document"/>
        <field name="groups" eval="[(4, ref('group_aidt_admin'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
    </record>

    <record id="rule_aidt_document_van_thu_cho_cap_so" model="ir.rule">
        <field name="name">Văn bản: Văn thư truy cập văn bản toàn cơ quan khi ở trạng thái Chờ cấp số hoặc Đã ban hành</field>
        <field name="model_id" ref="model_aidt_document"/>
        <field name="groups" eval="[(4, ref('group_van_thu'))]"/>
        <field name="domain_force">['|', ('department_id', '=', False), '|', ('department_id', 'child_of', user.employee_id.department_id.ids), ('state', 'in', ['cho_cap_so', 'da_ban_hanh'])]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="rule_aidt_document_secrecy" model="ir.rule">
        <field name="name">Văn bản: chặn theo độ mật (toàn cục)</field>
        <field name="model_id" ref="model_aidt_document"/>
        <field name="domain_force">[('secrecy_level', '&lt;=', user.clearance_level)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</data>
</odoo>
