<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.form.aidt.meeting</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Biên bản cuộc họp" string="Biên bản cuộc họp"
                     name="aidt_meeting_minutes">
                    <block title="Dịch vụ AI" name="aidt_meeting_ai">
                        <setting string="Dịch vụ xử lý">
                            <field name="aidt_meeting_ai_service_url"/>
                        </setting>
                        <!-- Tách riêng khỏi ô "Dịch vụ xử lý" ở trên: trường
                             kia là ĐỊA CHỈ (đặt một lần rồi quên), còn ba
                             trường này là CHẤT LƯỢNG bản bóc băng — thứ
                             người vận hành sẽ quay lại chỉnh nhiều lần. -->
                        <setting string="Chất lượng bóc băng">
                            <field name="aidt_meeting_asr_ct2_model"/>
                            <field name="aidt_meeting_asr_language"/>
                            <field name="aidt_meeting_asr_prompt"/>
                        </setting>
                        <setting string="Tóm tắt">
                            <field name="aidt_meeting_llm_model"/>
                        </setting>
                    </block>
                    <block title="Chính sách" name="aidt_meeting_policy">
                        <setting string="Độ mật tối đa">
                            <field name="aidt_meeting_max_secrecy"/>
                        </setting>
                        <setting string="Lưu trữ audio">
                            <field name="aidt_meeting_audio_retention_days"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>
